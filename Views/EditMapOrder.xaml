<Window x:Class="hogs_gameEditor_wpf.Views.EditMapOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hogs_gameEditor_wpf.Views"
        mc:Ignorable="d"
        Title="EditMapOrder" Height="400" Width="600" Background="#FF191919" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid>
        <ListView x:Name="mapOrderListView" Margin="0,0,260,-7" Background="#202020" Foreground="White" FontSize="14" SelectionChanged="mapOrderListView_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">

                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Background" Value="#101010"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">

                                <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">

                                    <GridViewRowPresenter 
                                Columns="{Binding Path=View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                Content="{TemplateBinding Content}"/>
                                </Border>

                                <ControlTemplate.Triggers>

                                    <!-- Survol -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#303030"/>
                                    </Trigger>

                                    <!-- Sélection -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#646490"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>

                                    <!-- Désactivé -->
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="#555"/>
                                    </Trigger>

                                </ControlTemplate.Triggers>

                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>

                </Style>
            </ListView.ItemContainerStyle>




            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Map" DisplayMemberBinding="{Binding Map}" Width="100"/>

                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Background" Value="#FF202020"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                </GridView>
            </ListView.View>

            <ListView.Resources>
                <Style TargetType="ScrollBar" x:Key="VerticalScrollBarStyle">
                    <Setter Property="Width" Value="1"/>
                </Style>
            </ListView.Resources>


        </ListView>
        <ComboBox x:Name="mapNameReplacerComboBox" Height="25" Width="220"
          HorizontalAlignment="Left"
          Margin="350,100,0,0"
          FontSize="16"
          VerticalAlignment="Top"
          Background="#FF202020"
          Foreground="White"
          SelectionChanged="mapNameReplacerComboBox_SelectionChanged">

            <!-- Style pour la ComboBox elle-même -->
            <ComboBox.Style>
                <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static ToolBar.ComboBoxStyleKey}}"/>
            </ComboBox.Style>

            <!-- Style des items de la liste -->
        </ComboBox>

        <Button x:Name="validateSwapMapButton" Content="Swap Map" HorizontalAlignment="Left" Margin="350,140,0,0" VerticalAlignment="Top" Width="180" Click="validateSwapMapButton_Click" Foreground="White" Background="#FF1F1F1F"/>
        <Label Content="Select New Map to replace :" HorizontalAlignment="Left" Margin="380,70,0,0" VerticalAlignment="Top" Foreground="White"/>
        <Button x:Name="validateAllButton" Content="Validate Changes" HorizontalAlignment="Left" Margin="335,300,0,0" VerticalAlignment="Top" Width="240" Height="50" Foreground="White" Background="#1F1F1F" Click="validateAllButton_Click"/>
    </Grid>
</Window>
